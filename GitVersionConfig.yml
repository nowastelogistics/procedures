mode: ContinuousDeployment
assembly-informational-format: '{Major}.{Minor}.{Patch}-{PreReleaseLabel}-{ShortSha}'
branches:
  release:
    regex: '^release(s)?[/-]'
    mode: ContinuousDelivery
    label: rc
    increment: Inherit
    is-release-branch: false   # don't force “stable” behaviour
  master:
    regex: ^master$|^main$
    mode: ContinuousDeployment
    label: ''
    increment: Inherit
    is-release-branch: true
  develop:
    regex: dev(elop)?(ment)?$
    mode: ContinuousDelivery
    label: unstable
    increment: Inherit
  feature:
    regex: feature(s)?[/-]
    mode: ContinuousDelivery
    label: '{BranchName}'
    increment: Inherit
  hotfix:
    regex: hotfix(es)?[/-]
    mode: ContinuousDelivery
    label: beta
    increment: None
ignore:
  sha: []
merge-message-formats: {}